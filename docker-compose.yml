version: '3.8'
services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: voyage
      POSTGRES_PASSWORD: voyage_dev
      POSTGRES_DB: voyage_matcher
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
